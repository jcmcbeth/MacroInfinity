using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

using Infantry.Items;

namespace Parsers
{
    public class Program
    {
        public static void Main(string[] args)
        {   
            /* 
            FileStream stream;
            StreamReader reader;
            string line;
            string[] data;

            stream = File.Open("c:\\program files\\infantry\\WZA.itm", FileMode.Open, FileAccess.Read);
            reader = new StreamReader(stream);

            while ((line = reader.ReadLine()) != null)
            {
                data = Item.ParseLine(line);

                for (int i = 0; i < data.Length; i++)
                {
                    Console.WriteLine(data[i]);                    
                }

                Console.Read();
            }
            */
        }
    }
}
